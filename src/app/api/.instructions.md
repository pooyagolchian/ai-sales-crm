---
applyTo: "src/app/api/**"
---

# API Route Instructions

## Required in Every Route
```typescript
export const runtime = 'nodejs';
```

## Patterns
- Use `NextRequest` and `NextResponse` from `next/server`
- Return JSON responses with `NextResponse.json()`
- Use proper HTTP status codes: 200, 201, 400, 404, 500
- Error shape: `{ error: string, details?: string }`
- Get MCP client via `getMcpClient()` from `@/lib/mcp-client`
- Parse request bodies with `request.json()` and validate with Zod schemas
- MCP tool results come back as `{ content: [{ type: 'text', text: string }] }` — parse the text as JSON

## MCP Tool Names (v2)
- `notion-search` — search pages/databases
- `notion-query-database` — query a database with filters
- `notion-create-page` — create a new page in a database
- `notion-update-page` — update page properties
- `notion-retrieve-page` — get a page by ID
- `notion-retrieve-block-children` — get page content blocks
